"""Streamlit frontend for MARA.

Renders the chat interface, accepts a user research query, invokes the compiled
LangGraph pipeline, and streams intermediate agent outputs and the final
synthesised answer back to the user.
"""
